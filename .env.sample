# API Configuration
PORT=8000
HOST=0.0.0.0
DEBUG=True
ENVIRONMENT=development  # development, testing, production

# Security
SECRET_KEY=your-secret-key-here  # Generate a secure key for production
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000  # CORS origins

# File Upload Settings
MAX_FILE_SIZE=52428800  # 50MB in bytes
UPLOAD_DIR=./uploaded_files
STATIC_DIR=./static

# AI/Deepseek Configuration
DEEPSEEK_API_KEY=your-deepseek-api-key-here
DEEPSEEK_MODEL=deepseek-chat  # or your specific model name

# Database Configuration (if needed later)
# DATABASE_URL=postgresql://user:password@localhost:5432/market_pro

# Redis Configuration (for caching)
# REDIS_URL=redis://localhost:6379/0

# Logging
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_FILE=./logs/app.log

# Monitoring (for future use)
# PROMETHEUS_METRICS_PORT=9090
# GRAFANA_PORT=3000

# MCP Registry
MCP_REGISTRY_URL=http://localhost:8001  # URL for MCP service registry

# Frontend Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000  # API URL for frontend 